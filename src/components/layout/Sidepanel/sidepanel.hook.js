/*import { useState } from 'react';

export default () => {

    const [ sidepanel, setSidepanel ] = useState(false);
    const [ sidepanelContent, setSidepanelContent ] = useState(null);

    const defaultData = {
        color: 'primary',
        isCloseable: true
    };

    const handleSidepanel = (title, bodyContent, footerContent) => {
        setSidepanel(false);
        if (title) {
            setSidepanel(true);
            setDialogData(Object.assign({}, defaultData, data));
        }
        if (bodyContent)
    };

    return { sidepanel, handleSidepanel, dialogData };
};*/