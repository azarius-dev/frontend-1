import { Card } from '@core/components';
import { Section, Grid } from '@dapp/components';

const Dai = () => {
	return (
		<Section label="anon">
			<Grid>
				<Card gutter={30}>Test</Card>
				<Card gutter={30}>Test2</Card>
			</Grid>
		</Section>
	);
};

export default Dai;
